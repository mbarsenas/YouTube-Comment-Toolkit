import type { Metadata } from "next";
import { SeoLanding, type SeoPage } from "../seo-landing";

export const metadata: Metadata = { title: "YouTube Comment Exporter", description: "Export public YouTube comments to CSV after searching, filtering questions, or removing duplicate creators.", alternates: { canonical: "/youtube-comment-exporter" } };
const page: SeoPage = { path:"/youtube-comment-exporter", eyebrow:"YOUTUBE COMMENT EXPORTER", title:"Export YouTube comments", accent:"into a clean CSV.", intro:"Load comments from a public YouTube video, narrow them to the audience feedback you need, and export a spreadsheet-ready CSV.", cta:"Load comments", benefits:[{title:"Useful columns",copy:"Export comment author, text, likes, and posted time in a clean CSV that opens in spreadsheet tools."},{title:"Filter before export",copy:"Search by keyword or creator, keep questions only, or reduce the file to one comment per creator."},{title:"Private by design",copy:"Comment filtering happens in your browser, and CommentHarbor does not build a stored database of your comments."}], steps:[{title:"Paste a public video",copy:"CommentHarbor validates the URL and loads the available top-level comments."},{title:"Find the useful rows",copy:"Search the comment text, isolate questions, or remove duplicate creators before exporting."},{title:"Download the CSV",copy:"Pro users can save the filtered results as a portable CSV for research and follow-up."}], details:<>The exporter is built for focused research rather than a raw data dump. Clean the comment list in CommentHarbor, then download only the rows that match your purpose.</> };
export default function Page(){return <SeoLanding page={page}/>}
